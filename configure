#!/usr/bin/env gosh
;; Configuring WiLiKi
;; Run ./configure (or gosh ./configure) to generate Makefiles.

(use gauche.configure)

(cf-init-gauche-extension)

;; gettext translations
(cf-subst 'ALL_LINGUAS "ja vi")

(cf-output-default)

;; Local variables:
;; mode: scheme
;; end:
